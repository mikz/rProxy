
%table
  %thead
    %tr
      %th Key
      %th Value
      %td= link_to :new, new_plugin_setting_path
  %tbody
    - @settings.each do |v|
      %tr
        %td= v.name
        %td= v.value
        %td= button_to :destroy, plugin_setting_path(v.plugin_id, v), :method => :destroy